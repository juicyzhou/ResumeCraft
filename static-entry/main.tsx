import React from "react";
import ReactDOM from "react-dom/client";
import ResumeStudio from "../app/ResumeStudio";
import "../app/globals.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ResumeStudio />
  </React.StrictMode>,
);
